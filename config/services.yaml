values:
    scalar_value: 42

services:
    sensor_types:
        class: SensorTypeRepository
        module: nanobrew.domain.sensor_type_repository

    sensors:
        class: SqliteSensorRepository
        module: nanobrew.infrastructure.sqlite.sensor_repository
        dependencies:
            - sensor_types